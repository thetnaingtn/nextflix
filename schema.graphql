type Query {
  getShowCollection: Collection!
}

type Collection {
  trending: [Show!]!
  topRated: [Show!]!
  netflix: [Show!]!
  action: [Show!]!
  comedy: [Show!]!
  horror: [Show!]!
  romance: [Show!]!
  docs: [Show!]!
}

type Show {
  id: ID!
  overview: String
  original_language: String!
  original_title: String
}
