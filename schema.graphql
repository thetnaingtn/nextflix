type Query {
  getHero: Hero!
}

type Hero {
  id: ID!
  title: String!
  overview: String!
  poster: String
  matchPercentage: String!
  releaseDate: String
  language: String!
  genres: [String!]!
  trailer: String
}
